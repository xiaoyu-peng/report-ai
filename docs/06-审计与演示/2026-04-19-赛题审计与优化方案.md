# 第二站 ReportAI — 赛题审计 + 优化方案

**日期**：2026-04-19
**对标参考**：晴天大模型报告（`docs/晴天大模型报告/01 需求文档.docx`、`03 使用指南.docx`）
**赛题**：T5 智能报告写作平台——知识库生成、以稿写稿（5 模块、5 维度评分共 100%）

---

## 一、对赛题 5 模块的当前状态审计

以下按赛题 5 模块逐条勾对，标注 ✅ 已具备 / ⚠️ 有缺陷 / ❌ 缺失。

### 模块一 · 知识库构建与管理

| 赛题条目 | 当前状态 | 缺陷/问题 |
|---|---|---|
| 批量导入 PDF/Word/TXT/Markdown | ✅ `DocumentController.upload` + Tika 解析 | 单篇上传（前端 multiple 只是浏览器多选），未实现"一次解析一包" |
| 协议外挂知识库（MCP / HTTP） | ✅ URL 抓取 `POST /bases/{id}/url`（Jsoup） + 晴天 MCP / Tavily | 只有 URL 爬取算外挂，缺少对接任意第三方 RAG 服务的 **通用 MCP provider**（晴天原系统有"知识库 API"规范）|
| 按 4 类管理（政策/行业/历史/媒体）| ✅ `knowledge_base.category` + 前端筛选条 | **今日修前**，未建"历史报告"KB；现已补齐 |
| 知识库内容增删改查 | ⚠️ 增 ✅ / 删 ✅ / 改 ❌ → ✅（今日补）/ 查 ⚠️ | "改"原本完全缺失（今日已补 `PUT /documents/{id}` + 前端编辑 Dialog）；"查"只显示文件名列表、缺预览（今日已补查看 Dialog）|
| **乱码**（init.sql 种子数据）| ❌ → ✅（今日修复）| MySQL 客户端字符集错配导致 id=1/2/3 KB 名、4 条 demo 文档均乱码；今日已 `CONVERT latin1→utf8mb4` 还原 + 根治 compose 配置 |

### 模块二 · 知识检索与引用（核心 25%）

| 赛题条目 | 当前状态 | 缺陷/问题 |
|---|---|---|
| 自动检索相关内容 | ✅ `/api/v1/knowledge/search?kbId=&q=&topK=` MySQL FULLTEXT | 仅全文匹配，没有语义召回；中文长 query 召回精度偏弱 |
| 溯源：文档名 + 页码/段落 | ✅ `knowledge_chunk.page_start/end` + API 返回 `chunkIndex` | PDF 有页码，URL/Word/TXT 无页码时前端不展示"第 1 页"是对的，但段号格式统一后更一目了然 |
| 手动补充/排除关键词 | ✅ `include_keywords / exclude_keywords` 列 + RAG 接口已支持 | 前端 **workspace** 已接入；**知识库详情页搜索** 未暴露高级筛选 |
| 【今日 bug】| ❌ → ✅ | 前端 `/bases/{id}/search?query=` 调错路由（应为 `/search?kbId=&q=`），详情页搜索框 400 ——  今日已修 `frontend/src/api/knowledge.ts` |

### 模块三 · AI 报告生成（25%）

| 赛题条目 | 当前状态 | 缺陷/问题 |
|---|---|---|
| 知识库驱动生成 | ✅ SSE 流 `GET /reports/{id}/generate` + RAG top-k | — |
| 仿写（学模板大纲/风格） | ⚠️ 有 `report_template` 表和 `structure_json / style_description` 字段，但"模板学习"只在创建时选模板，未实现"上传 PDF → LLM 解析 → 生成可复用模板" | `docs/以稿写稿-报告样例` 6 份 PDF 没有自动入库成模板 |
| 大纲保存复用 | ⚠️ `report_template.structure_json` 存大纲，但前端"模板中心"仅列出内置 3 条，**无新建/编辑 UI** | 用户造的大纲没法保存 |
| 事实/引用/覆盖度检查 | ❌ | 完全缺失 —— 这是 25% 评分明确提到的"质量保障" |

### 模块四 · 以稿写稿/智能改写（核心 25%）

| 赛题条目 | 当前状态 | 缺陷/问题 |
|---|---|---|
| 解析结构 | ⚠️ 仅"引用模板 structure_json"；没有"上传旧稿 → 自动解析章节" | 晴天的核心流程"选模板 → 输入主题"我们做到了，但**"上传旧稿 → 智能识别大纲"** 缺位 |
| 4 种改写模式 | ✅ 后端 `RewriteMode` enum 4 档 + `Prompts` 实现 | — |
| 续写（CONTINUATION 第 5 模式）| ✅ 今天早上补的（commit `dff1637`）| — |
| 修订视图（diff 对比）| ⚠️ 有 `report_version` + SSE diff 流，但**数据来源叙事只显示最新版对比前版**，晴天要"新增绿/删除红"级差异高亮 | 前端是 unified diff 文本，**不是 side-by-side 高亮**；差距明显 |

### 模块五 · 版本管理与导出

| 赛题条目 | 当前状态 | 缺陷/问题 |
|---|---|---|
| 多版本保存/回溯 | ✅ `report_version` + 回滚 API | — |
| 导出 Word | ✅ `ExportController /export/docx`（Apache POI 已接入） | 今天早上 commit 了 DocxExportService，但**没做端到端测试**；引用上标会否正确落地需验证 |
| 导出 PDF | ❌ | pom.xml 没依赖任何 PDF 库（iText / flying-saucer / wkhtmltopdf）——赛题明确要 Word/PDF 双导出，**PDF 完全缺失** |

### 跨模块 · 今天新发现的生产 Bug

1. `/logs/operations` 早先报 `Unknown column 'duration_ms'` —— 根因 `@TableField("duration")` 在旧 jar 中缺失；当前源码 `OperationLog.java:56` 已修，运行中不重现。
2. `/knowledge/bases/NaN/documents` —— detail.vue `route.params.id` 非数字时传 NaN；今日已加前端防御。
3. **RAG 搜索 500**（详情页搜索框）—— 前端路由与后端不一致；今日已修。
4. **duration 列写入失败**（历史）—— 同 #1，已恢复正常。
5. **Docker build 失败**（`mirror.ccs.tencentyun.com` 不可达）—— 基础镜像源问题，用户已要求改走 IDE。
6. **前端 Vite proxy 指 8081** —— 今日已改 `localhost:8080`，支持 IDE 跑。

---

## 二、对评分 5 维度的打分预估（保守）

| 维度 | 权重 | 现状得分预估 | 主要扣分点 |
|---|---|---|---|
| 知识检索准确性 | 25% | 17/25 | 语义召回缺 → 中长 query 相关性弱；引用页号统一度一般 |
| 报告生成质量 | 25% | 16/25 | 仿写风格还原度：**模板自动学习**缺，用户只能选内置 3 种 |
| 改写质量 | 25% | 18/25 | 4 种 + 续写都有，差异度 OK；但 diff 视图效果普通 |
| 对比展示与版本管理 | 15% | 9/15 | 版本列表 OK，diff 高亮视觉不够醒目 |
| 系统完整性与体验 | 10% | 6/10 | 乱码/NaN/搜索 500 等 bug 直接掉分；PDF 导出缺失 |
| **合计** | 100% | **~66/100** | |

**目标**：通过下面的优化方案冲到 **82+**。

---

## 三、优化方案（按优先级分 P0/P1/P2）

### P0 — 今日必须闭环（直接影响能否演示）

| # | 项 | 工作量 | 状态 |
|---|---|---|---|
| 1 | 乱码根治（init.sql + docker compose mysql command） | 0.5h | ✅ 已完成 |
| 2 | 文档查看/编辑接口 + 前端 Dialog | 1h | ✅ 已完成 |
| 3 | 详情页搜索 500 修复 | 0.2h | ✅ 已完成 |
| 4 | kbId=NaN 防御 | 0.2h | ✅ 已完成 |
| 5 | 一键启动脚本 `start.sh` / `stop.sh` + `.env.example` | 1h | 🟡 进行中 |
| 6 | 种子演示数据脚本（复用今天爬的 19 条权威源）| 0.5h | ⬜ 未开始 |

### P1 — 明后天闭环（直接拿分，对标晴天基线）

| # | 项 | 预期收益 | 工作量 |
|---|---|---|---|
| 7 | **模板自动学习**：上传 PDF → Claude/豆包抽 outline/tone → 存 `report_template` | +2 分（模块3 仿写）| 3h |
| 8 | **上传旧稿解析结构**（模块4 以稿写稿的入口）：PDF → LLM 抽章节树 → 作为"参考稿"进改写 | +3 分（模块4 核心）| 3h |
| 9 | **PDF 导出**：Word → PDF 流水线（libreoffice-cli 或 iText）| +1 分（模块5）| 2h |
| 10 | **Diff 视图升级**：前端用 `diff-match-patch` 渲染红/绿高亮，代替 unified text | +2 分（对比展示）| 2h |
| 11 | **报告引用溯源展示**：workspace 右侧已有 chunks 面板，但**预览视图**的 `[n]` 和溯源卡片联动滚动、高亮，要加视觉强化 | +1 分（检索准确性）| 1.5h |

### P2 — 有时间再做（差异化、加分项，超越晴天）

| # | 项 | 备注 |
|---|---|---|
| 12 | **语义检索混合召回**：当前 FULLTEXT 基础上叠加 keyword + embedding（本地 Ollama text-embedding 或豆包 embedding），top-k 重排 | 晴天也未做 |
| 13 | **事实性检查** Agent：报告生成后调二次 LLM，按 chunk 逐条 cross-check `[n]` 是否真命中 | 模块3 明确要 |
| 14 | **知识库覆盖度分析**：生成报告前，对主题做预检索 → 评估覆盖度 → 不足时提醒用户"要不要补充知识或开联网" | 晴天原系统的"数据不足自动走网搜"的本地版 |
| 15 | **报告分享（带批注）**：晴天已上线、我们没有；不做也行，但做了差异化明显 | 模块不硬要 |

---

## 四、按赛题交付清单核对

| 交付物 | 当前有无 | 备注 |
|---|---|---|
| 可直接打开的完整 Web 系统（5 模块可用） | ✅ 基本跑通 | 今日修完 bug 后本站 demo 通路无阻 |
| 3 份不同主题完整报告（含引用）| ⚠️ DB 里有 4 条 demo 报告，**但都是 init.sql 直插的假数据** | 需重新生成 3 份真 SSE 流出的报告 |
| 3 篇参考报告 × 4 模式改写展示 | ⚠️ `docs/以稿写稿-报告样例` 有 6 份 PDF，未入库成"参考稿" | 配合 P1#8 后补演示 |
| 至少 1 个续写展示 | ⚠️ CONTINUATION 模式已实现但没录演示 | 跑一次并截图即可 |
| 改写前后对比视图 | ✅ 基础版 | 依赖 P1#10 升级效果 |
| 设计说明文档 + AI 工具使用日志 | ✅ `AI-USAGE-LOG.md` 已维护 | 需要在每次 commit 后追加 |

---

## 五、下一步建议执行顺序

1. **今天剩余时间**：完成 P0#5 + P0#6（一键脚本 + 种子数据） → 演示场景就位
2. **明天 04-20**：P1#7 + P1#8 + P1#10（模板学习 + 旧稿解析 + diff 高亮）—— 拿模块 3/4/对比展示的 6-7 分
3. **04-21（赛前）**：P1#9（PDF 导出）+ 重新录 3 份演示报告 + 完成设计文档收尾

**预期冲分**：66 → **82-85 分**（P0+P1 全部吃下），若 P2 做任意 1 项再 +2 分。

---

*文档自动由当前会话审计产出，数据以 2026-04-19 14:45 时的系统快照为准。*
